<Window x:Class="AEAssist.View.Overlay.TriggerLineWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:viewModels="clr-namespace:AEAssist"
        mc:Ignorable="d"
        Topmost="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Title="{Binding Language.Title_LoadTriggerLine}" Height="200" Width="250">

    <Window.DataContext>
        <Binding Source="{x:Static viewModels:DataBinding.Instance}" />
    </Window.DataContext>


    <StackPanel Margin="10" Width="200">
        <StackPanel Orientation="Horizontal">
            <Button Name="LoadTriggerLine" Width="100" Height="30" Margin="2" Content="{Binding Language.Btn_LoadTriggerLine}" Click="LoadTriggerLine_OnClick" />
            <Button Name="ClearTriggerLine" Width="100" Height="30" Content="{Binding Language.Btn_ClearTriggerLine}" Click="ClearTriggerLine_OnClick" />
        </StackPanel>
        <Label Name="TriggerLineAuthor" Content="" />
        <Label Name="TriggerLineVersion" Content="" />
        <Label Name="TriggerLineTargetDuty" Content="" />
        <Label Name="TriggerLineJob" Content="" />
    </StackPanel>
</Window>